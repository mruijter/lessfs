lessfs requires tokyocabinet's tcutil library (TCTREE)
to be installed on the system for in-memory caching.
http://tokyocabinet.sourceforge.net/index.html

lessfs uses BerkeleyDB as its database backend.
LMDB support is planned as a future replacement.

To build from source:
./configure --with-berkeleydb
make
make install
